import { Container, SpinnerWrapper } from './Spinner.style'

const Spinner = () => {
  return (
    <Container data-testid="spinner">
      <SpinnerWrapper />
    </Container>
  )
}

export default Spinner
